# Hugeicons Pro token (needed to install @hugeicons-pro packages). See README.
HUGEICONS_TOKEN=

# Artificial Analysis API key (https://artificialanalysis.ai/api) — used by
# `npm run frontier:fetch` to refresh the frontier-intelligence chart dataset.
ARTIFICIAL_ANALYSIS_API_KEY=
